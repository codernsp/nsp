package com.nsp.messageProcessor.product;

/**
 * Enum holding fruit names ( assumption only contains fruits ), can be expanded if required.
 */
public enum ProductType {
    APPLE,
    BANANA,
    AVOCADO,
    MANGO,
    GRAPE;
}
